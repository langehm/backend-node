apiVersion: v1
kind: Service
metadata:
  name: {{ include "backend-node-chart.fullname" . }}
  labels:
    {{- include "backend-node-chart.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "backend-node-chart.selectorLabels" . | nindent 4 }}
